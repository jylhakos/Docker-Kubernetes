apiVersion: v1
kind: Service
metadata:
  name: gke-demo-service
  labels:
    app: gke-demo
spec:
  type: LoadBalancer
  selector:
    app: gke-demo
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
